{
  "name": "TeamBuilder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/index.html --no-minify --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:venelinn/buildteam.git"
  },
  "keywords": [],
  "author": "Venelin",
  "license": "MIT",
  "dependencies": {
    "data": "^0.6.1",
    "imagesloaded": "^4.1.4",
    "parcel-bundler": "^1.12.4"
  },
  "devDependencies": {
    "sass": "^1.26.3"
  }
}
